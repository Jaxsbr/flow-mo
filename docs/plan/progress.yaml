# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 5
  phase: flow-mo-ide-extension
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-F2] Investigate: confirm app imports to rewire from src/lib and src/types to @flow-mo/core, check if src/lib and src/types can be deleted"
  consecutive_fails: 0
  completed_stories:
    - US-F1
