# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 10
  phase: flow-mo-ide-extension
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-F4] Implement webview React app with VS Code message bridge, validation error display, external change notification, fitView, disposal"
  consecutive_fails: 0
  completed_stories:
    - US-F1
    - US-F2
