# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 5
  phase: smart-edge-routing
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-R2] Implement smart edge rendering — integrate pathfinding into FlowMoEdge with useNodes for obstacles, useMemo for caching, getSmoothStepPath fallback, and geometric midpoint for labels"
  consecutive_fails: 0
  completed_stories:
    - US-R1
