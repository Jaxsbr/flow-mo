# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 6
  phase: editor-discoverability
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-D2] Add newFlow command to package.json (contributes.commands + explorer/context menu) and implement command handler in extension.ts — create template file with auto-increment, open in custom editor"
  consecutive_fails: 0
  completed_stories:
    - US-D1
