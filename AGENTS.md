# FlowMo

## Purpose
Two-way YAML-to-visual-diagram VS Code/Cursor extension. Allows agents (YAML) and humans (visual diagram) to communicate and design complex flows. Exposes the extension as an MCP server for other agents to use.

## Quality checks

- no-silent-pass
- no-bare-except
- error-path-coverage
- agents-consistency
